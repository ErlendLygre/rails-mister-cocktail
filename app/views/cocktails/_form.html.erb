<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name, label: false, placeholder: 'Input name' %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>
